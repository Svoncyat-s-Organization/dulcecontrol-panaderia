import { Typography } from 'antd';

const { Title } = Typography;

const DirectorioDominiosPage = () => {
    return (
        <Title level={1}>
            Directorio de Dominios
        </Title>
    );
};

export default DirectorioDominiosPage;