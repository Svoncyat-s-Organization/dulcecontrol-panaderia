import { Typography } from 'antd';

const { Title } = Typography;

const DirectorioSedesPage = () => {
    return (
        <Title level={1}>
            Directorio de Sedes
        </Title>
    );
};

export default DirectorioSedesPage;