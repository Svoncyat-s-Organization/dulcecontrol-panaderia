import { Typography } from 'antd';

const { Title } = Typography;

const DirectorioUsuariosPage = () => {
    return (
        <Title level={1}>
            Directorio de Usuarios
        </Title>
    );
};

export default DirectorioUsuariosPage;